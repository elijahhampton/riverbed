[package]
name = "riverbed"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1.92"
axum = { version = "0.8.9", optional = true, features = ["http2", "macros", "ws"] }
once_cell = "1.21.4"
prost = { version = "0.14.4", optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2.0.20"
tokio = { version = "1.53.1", features = ["full"] }
tonic = { version = "0.14.6", optional = true, default-features = false, features = ["codegen", "router", "server"] }
tonic-prost = { version = "0.14.6", optional = true }
tracing = "0.1.44"
uuid = { version = "1.26.1", features = ["serde", "v4"] }

[features]
default = ["in-memory", "rest-api"]
in-memory = []
rest-api = ["dep:axum"]
grpc = ["dep:prost", "dep:tonic", "dep:tonic-prost", "dep:tonic-prost-build"]

[dev-dependencies]
tracing-subscriber = { version = "0.3.23", features = ["env-filter"] }

[build-dependencies]
tonic-prost-build = { version = "0.14.6", optional = true }
